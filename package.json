{
  "name": "basitclaw",
  "version": "0.4.0",
  "private": true,
  "description": "Workforce internal audit assurance and governance workspace.",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "lint": "node --check src/server.js && node --check src/services/workforceAuditService.js && node --check src/services/workforceAuditRegistry.js && node --check src/services/governanceLedger.js && node --check src/security/accessControl.js && node --check src/persistence/encryptedSnapshotStore.js && node --check src/persistence/backupManager.js",
    "build": "node scripts/verify-build.js"
  }
}
